$(document).ready(() => {
  const eventRecommender = new EventRecommender();
  // Your code here
});

function displayUsers(users) {
  users.forEach((id = "all-users")); // this is the id I needed from html
}
